<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.florent37.user">

    <application>
        <activity android:name=".UserActivity"/>

        <provider
            android:authorities="${applicationId}.user.RouteProvider"
            android:name=".RouteProvider"/>

        <provider
            android:authorities="${applicationId}.user.DependenciesProvider"
            android:name=".DependenciesProvider"/>
    </application>

</manifest>